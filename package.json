{
  "name": "rollup-starter-code-splitting",
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "rollup": "^1.0.0",
    "serve": "^10.1.1"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  }
}
