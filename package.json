{
  "name": "rollup-starter-code-splitting",
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "rollup": "^0.64.1",
    "serve": "^6.5.8"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  }
}
