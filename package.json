{
  "name": "rollup-starter-code-splitting",
  "devDependencies": {
    "npm-run-all": "^4.1.2",
    "rollup": "^0.57.1"
  },
  "dependencies": {
    "serve": "^6.5.3"
  },
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c -w",
    "dev": "npm-run-all --parallel start watch",
    "start": "serve public"
  }
}
